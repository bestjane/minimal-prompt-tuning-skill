# minimal-prompt-tuning-skill

A Codex skill for low-token prompt and tool tuning based on badcases.

## Install

Copy this folder to `~/.codex/skills/minimal-prompt-tuning`.

## Use

Invoke with `$minimal-prompt-tuning` in Codex.
